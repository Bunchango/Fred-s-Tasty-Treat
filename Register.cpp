#include "Register.h"

Register::Register() {
  // Create a default balance with no money
}

int Register::pay(int cost, int denomination) { return 0; }

bool Register::enoughDenomination(int denomination) { return true; }
