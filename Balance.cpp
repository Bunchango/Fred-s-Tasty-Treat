#include "Balance.h"

Balance::Balance() {
  // Create a default balance with no money
}

int Balance::pay(int cost, int denomination) { return 0; }

bool Balance::enoughDenomination(int denomination) { return true; }

void Balance::insert(Coin coin) {}
